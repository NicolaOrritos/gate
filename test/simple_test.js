/*global describe, it */
'use strict';

var assert = require('assert');


// 1. Start target fake server
// 2. Start gate proxy
// 3. Start testing
// 4. Cleanup files


describe('"gate" node module', function () {
  it('must have at least one test', function () {
    assert(false, 'Still too early to write any tests. Coming soon...');
  });
});
